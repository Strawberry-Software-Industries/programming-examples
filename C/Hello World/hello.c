#include <stdio.h> 

char* str_en = "Hello World!";
char* str_de = "Hallo Welt!";
char* lang = "de";

int main(void) {
    if (lang = "de") {
        printf(str_de);
    }
    else
    {
        printf(str_en);
    }
    

    return 0;
}